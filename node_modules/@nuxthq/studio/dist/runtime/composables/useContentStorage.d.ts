import type { ParsedContent } from '@nuxt/content/types';
import type { PreviewFile } from '../types.js';
export declare const useContentStorage: () => {
    storage: any;
    findContentItem: (path: string) => Promise<ParsedContent | null>;
    updateContentItem: (previewToken: string, file: PreviewFile) => void;
    removeContentItem: (previewToken: string, path: string) => Promise<void>;
    removeAllContentItems: (previewToken: string) => Promise<void>;
    setPreviewMetaItems: (previewToken: string, files: PreviewFile[]) => Promise<void>;
};
